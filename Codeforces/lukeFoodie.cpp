#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main()
{
    ll q;
    cin >> q;
    for (ll t = 0; t < q; t++)
    {
        ll n, x = 0;
        cin >> n >> x;
        vector<ll> food;
        for (int i = 0; i < n; i++)
        {
            ll num;
            cin >> num;
            food.push_back(num);
        }
        }
    return 0;
}